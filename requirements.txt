# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-multipart==0.0.12

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.35

# AI/ML Services
anthropic>=0.25.0
google-generativeai==0.8.3
openai==1.54.3
pinecone-client==5.0.1

# Data Processing
numpy==1.26.4
pandas==2.2.3

# HTTP Client
httpx==0.27.2
requests>=2.31.0

# MCP SDK (Plan B WP5 §11.4 — real wire-protocol MCP for external clients)
# Pinned to 0.9.1 because newer mcp versions require starlette>=0.39, which
# conflicts with our fastapi==0.115.0 pin (fastapi 0.115 requires starlette
# <0.39). Bump together when FastAPI is upgraded.
mcp==0.9.1
sse-starlette>=2.0,<4.0

# Semantic exports (Gate 2C)
rdflib==7.6.0                     # Turtle/JSON-LD graph serializer (runtime)
pyld==3.0.0                       # Gate 2C export validators (test-time): JSON-LD expand/normalize
pyoxigraph==0.5.8                 # Gate 2C export validators (test-time): independent triple store + SPARQL
dbt-semantic-interfaces==0.10.5   # Gate 2C export validators (test-time): MetricFlow spec parser (pydantic<3 — no conflict with pydantic==2.9.2)

# Scheduler (Gate 3B D1 — APScheduler for structural-drift monitor)
apscheduler>=3.10,<4.0

# Utilities
python-dotenv==1.0.1
pyyaml>=6.0
pyhumps>=3.8.0
redis
tenacity

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
